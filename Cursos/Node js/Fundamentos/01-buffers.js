// una tira de ByteLengthQueuingStrategy(datos binarios)
// similar a un array de enteros
// tamaño fijo
// manipular datos directamentes
'use strict'
// import{ Buffer } from ('buffer')
let buf = new Buffer(50);
console.log(buf);